export function page(maxLength: number): string {
return String.raw`<!doctype html>
<html lang="en" data-theme="light"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>DWC Distiller | Doctors Who Code</title>
<style>
@font-face{font-family:'DM Sans';font-weight:400;font-display:swap;src:url('https://www.doctorswhocode.blog/_astro/dm-sans-latin-400-normal.CW0RaeGs.woff2') format('woff2')}
@font-face{font-family:Syne;font-weight:800;font-display:swap;src:url('https://www.doctorswhocode.blog/_astro/syne-latin-800-normal.EehdNYzx.woff2') format('woff2')}
@font-face{font-family:'DM Sans';font-weight:600;font-display:swap;src:url('https://www.doctorswhocode.blog/_astro/dm-sans-latin-600-normal.Aqo67rzb.woff2') format('woff2')}
:root{color-scheme:light;--bg:#f8fafc;--card:#fff;--ink:#142033;--muted:#475569;--accent:#03638c;--purple:#7540b4;--line:#cbd5e1;--soft:#eef5fa;--error:#a32929}
[data-theme=dark]{color-scheme:dark;--bg:#0a0d1a;--card:#0f1219;--ink:#f8fafc;--muted:#a8b6ca;--accent:#38bdf8;--purple:#c084fc;--line:#394457;--soft:#141f30;--error:#ffaaaa}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:16px/1.65 'DM Sans',system-ui,sans-serif}a{color:inherit}button,textarea{font:inherit}button,a,textarea{touch-action:manipulation}button{cursor:pointer}button:disabled{opacity:.6;cursor:wait}a:focus-visible,button:focus-visible,textarea:focus-visible{outline:3px solid var(--accent);outline-offset:4px}.wrap{max-width:1180px;margin:auto;padding:0 28px}nav{border-bottom:1px solid var(--line);background:var(--card)}.nav-inner{display:flex;align-items:center;justify-content:space-between;gap:20px;min-height:78px}.brand{display:flex;gap:12px;align-items:center;text-decoration:none;font-family:Syne,system-ui;font-weight:800}.mark{font:700 13px monospace;color:var(--accent);border:1px solid var(--accent);padding:4px 8px;border-radius:5px}.nav-actions{display:flex;align-items:center;gap:24px}.nav-actions a{font:13px monospace;color:var(--muted);text-decoration:none}.secondary{border:1px solid var(--line);background:var(--card);color:var(--ink);border-radius:8px;padding:8px 12px;font-size:13px;min-height:40px}.intro{padding:48px 0 32px;display:flex;justify-content:space-between;align-items:center;gap:35px}.eyebrow{font:12px monospace;color:var(--accent);text-transform:uppercase;letter-spacing:.12em}h1,h2,h3{font-family:Syne,system-ui,sans-serif;font-weight:800;line-height:1.15}h1{font-size:clamp(38px,5vw,58px);letter-spacing:-.04em;margin:14px 0}h1 span{color:var(--accent)}.intro p{color:var(--muted);max-width:620px;margin:12px 0 0;font-size:17px}.build-note{border-left:2px solid var(--accent);padding-left:18px;min-width:200px;font:12px/1.8 monospace;color:var(--muted)}.build-note strong{display:block;color:var(--ink);font-size:14px}.workspace{display:grid;grid-template-columns:1fr 1fr;border:1px solid var(--line);border-radius:16px;overflow:hidden;background:var(--card);box-shadow:0 14px 40px #14203306}.panel{padding:28px;min-width:0}.panel+ .panel{border-left:1px solid var(--line);background:linear-gradient(145deg,var(--card),var(--soft))}.panel-head{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:22px}.panel-head h2{font-size:20px;margin:0}.number{font:12px monospace;color:var(--accent);margin-right:8px}.tag{font:11px monospace;color:var(--accent);background:var(--soft);border:1px solid var(--line);border-radius:30px;padding:4px 10px}label{display:block;font-size:13px;font-weight:600;margin-bottom:10px}textarea{width:100%;min-height:280px;resize:vertical;border:1px solid var(--line);background:var(--bg);color:var(--ink);border-radius:10px;padding:16px;line-height:1.65;font-size:15px}textarea::placeholder{color:var(--muted)}.meta{display:flex;justify-content:space-between;gap:10px;font:11px monospace;color:var(--muted);margin:10px 0 22px}.actions{display:flex;gap:10px;flex-wrap:wrap}.primary{border:0;border-radius:8px;background:var(--accent);color:var(--card);font-weight:700;padding:12px 22px;min-height:46px}.note{font-size:12px;line-height:1.65;color:var(--muted);margin:18px 0 0}.empty{text-align:center;padding:48px 15px 42px;color:var(--muted)}.empty-icon{display:grid;place-items:center;width:60px;height:60px;margin:0 auto 22px;border:1px solid var(--line);border-radius:14px;color:var(--accent);background:var(--card);font:26px monospace}.empty h3{color:var(--ink);font-size:23px;margin:0 0 12px}.empty p{font-size:14px;max-width:290px;margin:auto}.result-label{font:12px monospace;color:var(--accent)}.excerpt{white-space:pre-wrap;overflow-wrap:anywhere;font-size:16px;line-height:1.8;border-left:3px solid var(--accent);padding-left:18px;margin:22px 0}.stats{display:flex;gap:32px;border-top:1px solid var(--line);padding:18px 0}.stats strong{display:block;font:800 24px Syne,system-ui}.stats span{font:11px monospace;color:var(--muted)}.error{color:var(--error);border-left:3px solid var(--error);padding:8px 12px;font-size:14px}.status{font-size:13px;color:var(--muted)}.explainer{display:grid;grid-template-columns:repeat(3,1fr);gap:28px;margin:32px 0 42px}.explainer h3{font-size:14px;margin:0 0 8px}.explainer p{font-size:13px;color:var(--muted);margin:0}footer{border-top:1px solid var(--line);padding:22px 0 30px;display:flex;justify-content:space-between;gap:20px;color:var(--muted);font:11px/1.8 monospace}[hidden]{display:none!important}.skip{position:absolute;left:10px;top:-100px;background:var(--card);padding:10px}.skip:focus{top:10px;z-index:3}@media(max-width:760px){.wrap{padding:0 18px}.nav-inner{min-height:68px}.nav-actions{gap:10px}.nav-actions a{display:none}.brand{font-size:13px}.intro{padding:32px 0 24px}.build-note{display:none}.workspace{grid-template-columns:1fr}.panel{padding:22px}.panel+.panel{border-left:0;border-top:1px solid var(--line)}.explainer{grid-template-columns:1fr;gap:20px}.empty{padding:28px 10px}textarea{min-height:240px}footer{flex-direction:column;gap:6px}}@media(prefers-reduced-motion:no-preference){button{transition:filter .15s}button:hover{filter:brightness(.93)}}
</style></head><body><a class="skip" href="#source">Skip to text input</a>
<nav aria-label="Main navigation"><div class="wrap nav-inner"><a class="brand" href="https://www.doctorswhocode.blog/"><span class="mark">DWC</span>Doctors Who Code</a><div class="nav-actions"><a href="https://www.doctorswhocode.blog/">Back to the academy ↗</a><button class="secondary" id="theme" type="button" aria-label="Switch to dark theme">Dark theme</button></div></div></nav>
<main class="wrap"><header class="intro"><div><div class="eyebrow">Physician-built tools / 001</div><h1>DWC <span>Distiller.</span></h1><p>Bring your text. Start with a clearer view.<br>A focused workspace for articles, transcripts, and notes.</p></div><div class="build-note"><strong>A working first step.</strong>Text preview available<br>AI summarization comes next</div></header>
<div class="workspace"><section class="panel" aria-labelledby="input-title"><div class="panel-head"><h2 id="input-title"><span class="number">01</span>Your source</h2><button class="secondary" type="button" id="sample">Try an example</button></div>
<form id="distill-form" novalidate><label for="source">Paste the text you want to explore</label><textarea id="source" aria-describedby="counter privacy" placeholder="Paste an article excerpt, a transcript, or your working notes here…"></textarea><div class="meta"><span id="counter">0 / 10,000 characters</span><span>Plain text</span></div><div class="actions"><button class="primary" id="submit" type="submit">Distill text →</button><button class="secondary" id="clear" type="button">Clear</button></div><p id="error" class="error" role="alert" hidden></p><p id="privacy" class="note">Use public or nonconfidential text. Do not paste patient information. This prototype processes your text without an AI model.</p></form></section>
<section class="panel" id="result-panel" aria-labelledby="result-title"><div class="panel-head"><h2 id="result-title"><span class="number">02</span>Your result</h2><span class="tag">TEXT PREVIEW</span></div><p id="status" class="status" role="status" aria-live="polite"></p><div id="empty" class="empty"><div class="empty-icon" aria-hidden="true">{ }</div><h3>A place for the main idea.</h3><p>Your text preview will appear here. Start with your own passage or try the example.</p></div><div id="result" hidden><div class="result-label" id="result-label"></div><p class="excerpt" id="excerpt"></p><div class="stats"><div><strong id="words"></strong><span>WORDS IN SOURCE</span></div><div><strong id="characters"></strong><span>CHARACTERS</span></div></div><button class="secondary" id="copy" type="button">Copy preview</button><p class="note">This excerpt shows the beginning of your passage. It is not a summary or an assessment of the source.</p></div></section></div>
<div class="explainer"><section><h3>01 / Start with the source.</h3><p>Paste up to 10,000 characters. Your original text stays available for comparison.</p></section><section><h3>02 / Inspect the result.</h3><p>Review the opening excerpt and word count. The processing method is always visible.</p></section><section><h3>03 / Keep the judgment.</h3><p>A shorter passage is only a starting point. Return to the source for context.</p></section></div><noscript><p>Enable JavaScript to submit text and view your preview.</p></noscript><footer><span>Doctors Who Code · Where medicine meets code.</span><span>DWC Distiller / Prototype 01 / Text preview</span></footer></main>
<script>
const $ = (id) => document.getElementById(id);
const source = $('source'), button = $('submit'), result = $('result'), error = $('error');
const limit = ${maxLength};
let busy = false;
function changed() { $('counter').textContent = source.value.length.toLocaleString() + ' / 10,000 characters'; error.hidden = true; source.removeAttribute('aria-invalid'); result.hidden = true; $('empty').hidden = false; $('status').textContent = ''; $('copy').textContent = 'Copy preview'; }
source.addEventListener('input', changed);
$('sample').addEventListener('click', () => { source.value = 'A useful software project begins with a bounded problem. Build the smallest complete path from input to output, then test it with real examples. Keep a record of what worked, what failed, and what changed. Add complexity only after the baseline is reliable. A working system gives the next decision something concrete to build on.'; changed(); source.focus(); });
$('clear').addEventListener('click', () => { source.value = ''; changed(); source.focus(); });
$('theme').addEventListener('click', () => { const dark = document.documentElement.dataset.theme !== 'dark'; document.documentElement.dataset.theme = dark ? 'dark' : 'light'; $('theme').textContent = dark ? 'Light theme' : 'Dark theme'; $('theme').setAttribute('aria-label', dark ? 'Switch to light theme' : 'Switch to dark theme'); });
$('copy').addEventListener('click', async () => { try { await navigator.clipboard.writeText($('result-label').textContent + '\n\n' + $('excerpt').textContent + '\n\n' + $('words').textContent + ' words in source'); $('copy').textContent = 'Copied'; $('status').textContent = 'Preview copied.'; } catch { $('status').textContent = 'Copy is unavailable. Select the preview text and copy it manually.'; } });
$('distill-form').addEventListener('submit', async (event) => {
 event.preventDefault(); if (busy) return; changed(); const text = source.value;
 if (!text.trim() || text.length > limit) { error.textContent = !text.trim() ? 'Paste some text before selecting Distill.' : 'Use at most 10,000 characters. Shorten your passage and try again.'; error.hidden = false; source.setAttribute('aria-invalid', 'true'); source.focus(); return; }
 busy = true; button.disabled = true; source.readOnly = true; $('sample').disabled = true; $('clear').disabled = true; button.textContent = 'Distilling…'; $('result-panel').setAttribute('aria-busy', 'true'); $('empty').hidden = true; $('status').textContent = 'Creating your text preview…';
 const controller = new AbortController(); const timer = setTimeout(() => controller.abort(), 20000);
 try {
  const response = await fetch('/api/distill', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text }), signal: controller.signal });
  let data; try { data = await response.json(); } catch { throw new Error('The server returned an unreadable response. Please try again.'); }
  if (!response.ok) throw new Error(typeof data?.error === 'string' ? data.error : 'The server could not process your text. Please try again.');
  if (!data || typeof data.excerpt !== 'string' || typeof data.label !== 'string' || !Number.isFinite(data.wordCount) || !Number.isFinite(data.characterCount)) throw new Error('The response was incomplete. Please try again.');
  $('result-label').textContent = data.label; $('excerpt').textContent = data.excerpt; $('words').textContent = data.wordCount.toLocaleString(); $('characters').textContent = data.characterCount.toLocaleString(); result.hidden = false; $('status').textContent = 'Your text preview is ready.';
 } catch (failure) { error.textContent = failure.name === 'AbortError' ? 'The request took too long. Please try again.' : failure instanceof TypeError ? 'Could not reach the server. Check your connection and try again.' : failure.message; error.hidden = false; $('status').textContent = 'No preview was generated. Your source text is preserved.'; }
 finally { clearTimeout(timer); busy = false; button.disabled = false; source.readOnly = false; $('sample').disabled = false; $('clear').disabled = false; button.textContent = 'Distill text →'; $('result-panel').removeAttribute('aria-busy'); }
});
</script></body></html>`;
}
